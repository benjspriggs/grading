language: bash
sudo: required
install:
        - ./install.sh
before_script:
        - sudo add-apt-repository ppa:duggan/bats --yes
        - sudo apt-get update
        - sudo apt-get install bats
script:
        - bash run-tests.sh
