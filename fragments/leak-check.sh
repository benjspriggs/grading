#!/usr/bin/env bash
# fragments/leak-check.sh
# Makes sure a program has no leaks
# based on the first argument given (executable name)
# Errors are dumped into a valgrind_output.txt,
# then appended to a student's report

