#!/usr/bin/bash
# fragments/spellcheck.sh
# Spellcheck a bunch of files

# make sure we have the student report

# make sure we have the files to spellcheck
# (and that they're spellcheckable)

# check a .cpp file...
# aspell -t check --mode=ccpp --run-together $file.cpp
